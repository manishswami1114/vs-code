#include<iostream>
using namespace std;
void mergesort(int arr[],int i,int j){
    int i=1,j=m+1;
}
int mergesort(int arr[],int l,int h){
    if(l<h){
        return 
    }
    else{
        int m=(l+h)/2;
    mergesort(arr,l,m);
    mergesort(arr,m+l,h);
    merge(a,l,m,h);
    }
}
int main(){
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
}